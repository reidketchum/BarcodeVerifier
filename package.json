{
  "name": "local-barcode-verifier",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Node.js app for barcode verification with TUI",
  "main": "local-app.js",
  "scripts": {
    "start": "node local-app.js", 
    "dev": "node local-app.js"  // Can add nodemon later if needed
  },
  "dependencies": {
    "onoff": "^6.0.0",
    "mqtt": "^5.7.1"
    // Add a TUI library later, e.g.: "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/node": "^20"
    // Add other dev dependencies if needed
  }
}
